<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SoundFriends" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="colorPrimary">@color/primary_pink</item>
        <item name="colorPrimaryDark">@color/primary_pink_dark</item>
        <item name="android:navigationBarColor">@color/primary_pink</item>
    </style>

    <style name="Theme.SoundFriends" parent="Base.Theme.SoundFriends" />
</resources>